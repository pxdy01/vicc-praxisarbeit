# Vorlage für lokalen Test (docker run --env-file .env). Kopieren nach .env – .env wird nicht committet.
DB_HOST=vicc-pg-local
DB_PORT=5432
DB_NAME=tasks
DB_USER=postgres
DB_PASSWORD=<lokales-testpasswort>
DB_SSLMODE=disable
